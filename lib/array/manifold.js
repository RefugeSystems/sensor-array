
/**
 * Represents a schedulable selection of subprocessors to collect information.
 * @class Manifold
 * @constructor
 * @module SensorArray
 */
module.exports = function() {
	
	
	this.addSubprocessor = function(subprocessor) {
		
	};
	
	/**
	 * Collect data from all the underlying subprocessors
	 * @method aggregateData
	 * @param {Observer} observer 
	 */
	this.aggregateData = function(observer) {
		
	};
};
